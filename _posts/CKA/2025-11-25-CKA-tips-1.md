---
layout: post
title: "Tips-1"
date: 2026-01-25
categories:
  - "Certification/CKA"  
---

#### Reference
- [Udemy – Certified Kubernetes Administrator (CKA) with Practice Tests](https://www.udemy.com/course/certified-kubernetes-administrator-with-practice-tests/?couponCode=KEEPLEARNING)
(https://uklabs.kodekloud.com/topic/practice-test-replicasets-2/)

---
### NGINX Pod 생성

```bash 
kubectl run nginx --image=nginx
```

### POD 매니페스트 YAML 파일을 생성합니다(-o yaml). 생성하지 않습니다(--dry-run).

```bash
kubectl run nginx --image=nginx --dry-run=client -o yaml
```

### 배포를 생성하세요

```bash
kubectl create deployment --image=nginx nginx
```

### 배포 YAML 파일을 생성합니다(-o yaml). 생성하지 않습니다(--dry-run).

```bash
kubectl create deployment --image=nginx nginx --dry-run=client -o yaml
```

### 배포 YAML 파일을 생성합니다(-o yaml). 파일을 생성하지 않고(-dry-run) 파일로 저장하지도 않습니다.

```bash
kubectl create deployment --image=nginx nginx --dry-run=client -o yaml > nginx-deployment.yaml
```

### 파일에 필요한 변경 사항(예: 복제본 추가)을 적용한 다음 배포를 생성합니다.

```bash
kubectl create -f nginx-deployment.yaml
```

### Kubernetes 버전 1.19 이상에서는 `--replicas` 옵션을 지정하여 4개의 복제본으로 배포를 생성할 수 있습니다.

```bash
kubectl create deployment --image=nginx nginx --replicas=4 --dry-run=client -o yaml > nginx-deployment.yaml
```

---







